name: Deploy Servus Mobile

on:
  workflow_dispatch:
  push:
    branches:
      - 'build/stv/mobile/**'

jobs:
  servus_mobile:
    env:
      GITHUB_TOKEN: ${{ inputs.token }}
    uses: ./.github/workflows/deploy-variants.yml
    with:
      brand: "STV"
      platform: "Mobile"
