name: Use reusable workflow

on:
  pull_request:

jobs:
  example_job:
    name: Pass input and secrets to my-action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: reusable-workflow@main #./.github/workflows/reusable-workflow@main
        with:
          username: "SOBAKA"
